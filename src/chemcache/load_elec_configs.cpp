/*
 * This file is autogenerated by: generate_ptable_configs.py
 *
 * NOTE: Any modifications made in this file will be lost next time
 *       generate_ptable_configs.py is run.
 */

#include "chemcache/chemcache.hpp"

namespace chemcache {

void load_elec_configs(chemist::PeriodicTable& pt) {
    // Z =   0, Ez, ElementZero
    pt.add_elec_config(0, {0, 0, 0, 0});

    // Z =   1, H , Hydrogen
    pt.add_elec_config(1, {1, 0, 0, 0});

    // Z =   2, He, Helium
    pt.add_elec_config(2, {2, 0, 0, 0});

    // Z =   3, Li, Lithium
    pt.add_elec_config(3, {3, 0, 0, 0});

    // Z =   4, Be, Beryllium
    pt.add_elec_config(4, {4, 0, 0, 0});

    // Z =   5, B , Boron
    pt.add_elec_config(5, {4, 1, 0, 0});

    // Z =   6, C , Carbon
    pt.add_elec_config(6, {4, 2, 0, 0});

    // Z =   7, N , Nitrogen
    pt.add_elec_config(7, {4, 3, 0, 0});

    // Z =   8, O , Oxygen
    pt.add_elec_config(8, {4, 4, 0, 0});

    // Z =   9, F , Fluorine
    pt.add_elec_config(9, {4, 5, 0, 0});

    // Z =  10, Ne, Neon
    pt.add_elec_config(10, {4, 6, 0, 0});

    // Z =  11, Na, Sodium
    pt.add_elec_config(11, {5, 6, 0, 0});

    // Z =  12, Mg, Magnesium
    pt.add_elec_config(12, {6, 6, 0, 0});

    // Z =  13, Al, Aluminum
    pt.add_elec_config(13, {6, 7, 0, 0});

    // Z =  14, Si, Silicon
    pt.add_elec_config(14, {6, 8, 0, 0});

    // Z =  15, P , Phosphorus
    pt.add_elec_config(15, {6, 9, 0, 0});

    // Z =  16, S , Sulfur
    pt.add_elec_config(16, {6, 10, 0, 0});

    // Z =  17, Cl, Chlorine
    pt.add_elec_config(17, {6, 11, 0, 0});

    // Z =  18, Ar, Argon
    pt.add_elec_config(18, {6, 12, 0, 0});

    // Z =  19, K , Potassium
    pt.add_elec_config(19, {7, 12, 0, 0});

    // Z =  20, Ca, Calcium
    pt.add_elec_config(20, {8, 12, 0, 0});

    // Z =  21, Sc, Scandium
    pt.add_elec_config(21, {8, 13, 0, 0});

    // Z =  22, Ti, Titanium
    pt.add_elec_config(22, {8, 12, 2, 0});

    // Z =  23, V , Vanadium
    pt.add_elec_config(23, {8, 12, 3, 0});

    // Z =  24, Cr, Chromium
    pt.add_elec_config(24, {8, 12, 4, 0});

    // Z =  25, Mn, Manganese
    pt.add_elec_config(25, {6, 12, 7, 0});

    // Z =  26, Fe, Iron
    pt.add_elec_config(26, {6, 12, 8, 0});

    // Z =  27, Co, Cobalt
    pt.add_elec_config(27, {6, 12, 9, 0});

    // Z =  28, Ni, Nickel
    pt.add_elec_config(28, {6, 12, 10, 0});

    // Z =  29, Cu, Copper
    pt.add_elec_config(29, {7, 12, 10, 0});

    // Z =  30, Zn, Zinc
    pt.add_elec_config(30, {8, 12, 10, 0});

    // Z =  31, Ga, Gallium
    pt.add_elec_config(31, {8, 13, 10, 0});

    // Z =  32, Ge, Germanium
    pt.add_elec_config(32, {8, 14, 10, 0});

    // Z =  33, As, Arsenic
    pt.add_elec_config(33, {8, 15, 10, 0});

    // Z =  34, Se, Selenium
    pt.add_elec_config(34, {8, 16, 10, 0});

    // Z =  35, Br, Bromine
    pt.add_elec_config(35, {8, 17, 10, 0});

    // Z =  36, Kr, Krypton
    pt.add_elec_config(36, {8, 18, 10, 0});

    // Z =  37, Rb, Rubidium
    pt.add_elec_config(37, {9, 18, 10, 0});

    // Z =  38, Sr, Strontium
    pt.add_elec_config(38, {10, 18, 10, 0});

    // Z =  39, Y , Yttrium
    pt.add_elec_config(39, {10, 19, 10, 0});

    // Z =  40, Zr, Zirconium
    pt.add_elec_config(40, {10, 18, 12, 0});

    // Z =  41, Nb, Niobium
    pt.add_elec_config(41, {10, 18, 13, 0});

    // Z =  42, Mo, Molybdenum
    pt.add_elec_config(42, {8, 18, 16, 0});

    // Z =  43, Tc, Technetium
    pt.add_elec_config(43, {8, 18, 17, 0});

    // Z =  44, Ru, Ruthenium
    pt.add_elec_config(44, {8, 18, 18, 0});

    // Z =  45, Rh, Rhodium
    pt.add_elec_config(45, {8, 18, 19, 0});

    // Z =  46, Pd, Palladium
    pt.add_elec_config(46, {8, 18, 20, 0});

    // Z =  47, Ag, Silver
    pt.add_elec_config(47, {9, 18, 20, 0});

    // Z =  48, Cd, Cadmium
    pt.add_elec_config(48, {10, 18, 20, 0});

    // Z =  49, In, Indium
    pt.add_elec_config(49, {10, 19, 20, 0});

    // Z =  50, Sn, Tin
    pt.add_elec_config(50, {10, 20, 20, 0});

    // Z =  51, Sb, Antimony
    pt.add_elec_config(51, {10, 21, 20, 0});

    // Z =  52, Te, Tellurium
    pt.add_elec_config(52, {10, 22, 20, 0});

    // Z =  53, I , Iodine
    pt.add_elec_config(53, {10, 23, 20, 0});

    // Z =  54, Xe, Xenon
    pt.add_elec_config(54, {10, 24, 20, 0});

    // Z =  55, Cs, Cesium
    pt.add_elec_config(55, {11, 24, 20, 0});

    // Z =  56, Ba, Barium
    pt.add_elec_config(56, {12, 24, 20, 0});

    // Z =  57, La, Lanthanum
    pt.add_elec_config(57, {12, 24, 21, 0});

    // Z =  58, Ce, Cerium
    pt.add_elec_config(58, {12, 24, 22, 0});

    // Z =  59, Pr, Praseodymium
    pt.add_elec_config(59, {12, 24, 21, 2});

    // Z =  60, Nd, Neodymium
    pt.add_elec_config(60, {12, 24, 20, 4});

    // Z =  61, Pm, Promethium
    pt.add_elec_config(61, {12, 24, 20, 5});

    // Z =  62, Sm, Samarium
    pt.add_elec_config(62, {12, 24, 20, 6});

    // Z =  63, Eu, Europium
    pt.add_elec_config(63, {12, 24, 20, 7});

    // Z =  64, Gd, Gadolinium
    pt.add_elec_config(64, {11, 24, 20, 9});

    // Z =  65, Tb, Terbium
    pt.add_elec_config(65, {10, 24, 20, 11});

    // Z =  66, Dy, Dysprosium
    pt.add_elec_config(66, {10, 24, 20, 12});

    // Z =  67, Ho, Holmium
    pt.add_elec_config(67, {10, 24, 20, 13});

    // Z =  68, Er, Erbium
    pt.add_elec_config(68, {10, 24, 20, 14});

    // Z =  69, Tm, Thulium
    pt.add_elec_config(69, {11, 24, 20, 14});

    // Z =  70, Yb, Ytterbium
    pt.add_elec_config(70, {12, 24, 20, 14});

    // Z =  71, Lu, Lutetium
    pt.add_elec_config(71, {12, 25, 20, 14});

    // Z =  72, Hf, Hafnium
    pt.add_elec_config(72, {12, 24, 22, 14});

    // Z =  73, Ta, Tantalum
    pt.add_elec_config(73, {12, 24, 23, 14});

    // Z =  74, W , Tungsten
    pt.add_elec_config(74, {10, 24, 26, 14});

    // Z =  75, Re, Rhenium
    pt.add_elec_config(75, {10, 24, 27, 14});

    // Z =  76, Os, Osmium
    pt.add_elec_config(76, {10, 24, 28, 14});

    // Z =  77, Ir, Iridium
    pt.add_elec_config(77, {10, 24, 29, 14});

    // Z =  78, Pt, Platinum
    pt.add_elec_config(78, {10, 24, 30, 14});

    // Z =  79, Au, Gold
    pt.add_elec_config(79, {11, 24, 30, 14});

    // Z =  80, Hg, Mercury
    pt.add_elec_config(80, {12, 24, 30, 14});

    // Z =  81, Tl, Thallium
    pt.add_elec_config(81, {12, 25, 30, 14});

    // Z =  82, Pb, Lead
    pt.add_elec_config(82, {12, 26, 30, 14});

    // Z =  83, Bi, Bismuth
    pt.add_elec_config(83, {12, 27, 30, 14});

    // Z =  84, Po, Polonium
    pt.add_elec_config(84, {12, 28, 30, 14});

    // Z =  85, At, Astatine
    pt.add_elec_config(85, {12, 29, 30, 14});

    // Z =  86, Rn, Radon
    pt.add_elec_config(86, {12, 30, 30, 14});

    // Z =  87, Fr, Francium
    pt.add_elec_config(87, {13, 30, 30, 14});

    // Z =  88, Ra, Radium
    pt.add_elec_config(88, {14, 30, 30, 14});

    // Z =  89, Ac, Actinium
    pt.add_elec_config(89, {14, 30, 31, 14});

    // Z =  90, Th, Thorium
    pt.add_elec_config(90, {14, 30, 32, 14});

    // Z =  91, Pa, Protactinium
    pt.add_elec_config(91, {14, 30, 30, 17});

    // Z =  92, U , Uranium
    pt.add_elec_config(92, {14, 30, 30, 18});

    // Z =  93, Np, Neptunium
    pt.add_elec_config(93, {14, 30, 30, 19});

    // Z =  94, Pu, Plutonium
    pt.add_elec_config(94, {13, 30, 30, 21});

    // Z =  95, Am, Americium
    pt.add_elec_config(95, {12, 30, 30, 23});

    // Z =  96, Cm, Curium
    pt.add_elec_config(96, {12, 30, 30, 24});

    // Z =  97, Bk, Berkelium
    pt.add_elec_config(97, {12, 30, 30, 25});

    // Z =  98, Cf, Californium
    pt.add_elec_config(98, {12, 30, 30, 26});

    // Z =  99, Es, Einsteinium
    pt.add_elec_config(99, {12, 30, 30, 27});

    // Z = 100, Fm, Fermium
    pt.add_elec_config(100, {12, 30, 30, 28});

    // Z = 101, Md, Mendelevium
    pt.add_elec_config(101, {13, 30, 30, 28});

    // Z = 102, No, Nobelium
    pt.add_elec_config(102, {14, 30, 30, 28});

    // Z = 103, Lr, Lawrencium
    pt.add_elec_config(103, {14, 30, 31, 28});

    // Z = 104, Rf, Rutherfordium
    pt.add_elec_config(104, {14, 30, 32, 28});

    // Z = 105, Db, Dubnium
    pt.add_elec_config(105, {14, 30, 33, 28});

    // Z = 106, Sg, Seaborgium
    pt.add_elec_config(106, {12, 30, 36, 28});

    // Z = 107, Bh, Bohrium
    pt.add_elec_config(107, {12, 30, 37, 28});

    // Z = 108, Hs, Hassium
    pt.add_elec_config(108, {12, 30, 38, 28});

    // Z = 109, Mt, Meitnerium
    pt.add_elec_config(109, {12, 30, 39, 28});

    // Z = 110, Ds, Darmstadtium
    pt.add_elec_config(110, {12, 30, 40, 28});

    // Z = 111, Rg, Roentgenium
    pt.add_elec_config(111, {13, 30, 40, 28});

    // Z = 112, Cn, Copernicium
    pt.add_elec_config(112, {14, 30, 40, 28});

    // Z = 113, Nh, Nihonium
    pt.add_elec_config(113, {14, 31, 40, 28});

    // Z = 114, Fl, Flerovium
    pt.add_elec_config(114, {14, 32, 40, 28});

    // Z = 115, Mc, Moscovium
    pt.add_elec_config(115, {14, 33, 40, 28});

    // Z = 116, Lv, Livermorium
    pt.add_elec_config(116, {14, 34, 40, 28});

    // Z = 117, Ts, Tennessine
    pt.add_elec_config(117, {14, 35, 40, 28});

    // Z = 118, Og, Oganesson
    pt.add_elec_config(118, {14, 36, 40, 28});

} // function load_elec_configs

} // namespace chemcache
